apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notification-service
  namespace: platform
spec:
  project: default
  source:
    repoURL: https://github.com/techxus/ibank-minikube-gitops.git
    targetRevision: main
    path: charts/notification-service
    helm:
      valueFiles:
        - ../../env/local/notification-service-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
